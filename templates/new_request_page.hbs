<div class="container-fullwidth gray">
    <nav class="sub-nav">
        {{breadcrumbs}}
        {{search submit=false}}
    </nav>
</div>
<div class="container">
  <h1>
    {{t 'submit_a_request'}}
    {{#if follow_up}}
      <span class="follow-up-hint">
        {{follow_up}}
      </span>
    {{/if}}
  </h1>

  <div class="form">
    {{request_form}}
  </div>
</div>
